#coding=utf-8

from utils import ReadFile
from config import *


if __name__ == '__main__':
    rf = ReadFile()
    # sinD = rf.countDownD(rf.countDownL(SinInPath))
    # mixD = rf.countDownD(rf.countDownL(MixInPath))
    # rf.countDownAll(rf.countDownL(SinPath), rf.countDownL(MixPath), AllOut)
    # rf.countDownAll(sinD, mixD, AllIn, AllNo)

    # rf.cutNoDump()
    # rf.ToFasta()
    # print(AllNo)
    # rf.get_geneId()
    # rf.get_protainId()
    #data = response.content.decode()
    #soup = BeautifulSoup(data, 'html.parser')
    #links = soup.find_all('div', class_="ncbi-doc-id")
    #link = links[0].find_all('ul', class_="ncbi-inline-list")[0].get_text()







